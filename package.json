{
  "name": "hosts-so-easy",
  "version": "1.1.1",
  "description": "Safe, parallel API for manipulating /etc/hosts",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/gadicc/hosts-so-easy"
  },
  "keywords": [
    "hosts",
    "hostfile",
    "hostsfile"
  ],
  "author": "Gadi Cohen <dragon@wastelands.net>",
  "license": "MIT",
  "dependencies": {
    "atomic-write": "^0.1.1",
    "eventemitter3": "^2.0.3",
    "lodash": "^4.17.4"
  },
  "scripts": {
    "build": "babel src -d lib",
    "coverage": "jest --coverage src",
    "coveralls": "jest --coverage src ; cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js",
    "dev": "nodemon src/index.js --exec babel-node",
    "lint": "eslint src",
    "prepare": "yarn build",
    "test": "jest src",
    "test-dist": "jest lib"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-eslint": "^8.0.2",
    "babel-preset-env": "^1.6.1",
    "coveralls": "^3.0.0",
    "eslint": "^4.11.0",
    "jest": "^21.2.1",
    "nodemon": "^1.12.1",
    "pify": "^3.0.0",
    "tmp": "^0.0.33"
  }
}
